// wengwengweng

use std::path::Path;
use std::path::PathBuf;

use crate::*;

pub struct ModelBuf;

impl ModelBuf {
	pub fn new(path: impl AsRef<Path>) -> Self {
		return Self;
	}
}

impl Buffer for ModelBuf {
	// ...
}

